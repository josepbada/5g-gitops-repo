apiVersion: v1
kind: ConfigMap
metadata:
  name: smf-html
  namespace: 5g-core
data:
  index.html: |
    SMF: 5G Session Management Function - Active
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smf
  namespace: 5g-core
  labels:
    app: smf
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smf
      version: v1
  template:
    metadata:
      labels:
        app: smf
        version: v1
    spec:
      containers:
      - name: smf
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "64Mi"
            cpu: "100m"
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: html
        configMap:
          name: smf-html
---
apiVersion: v1
kind: Service
metadata:
  name: smf-service
  namespace: 5g-core
  labels:
    app: smf
spec:
  selector:
    app: smf
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP