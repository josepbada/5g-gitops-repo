apiVersion: v1
kind: ConfigMap
metadata:
  name: nrf-html
  namespace: 5g-core
data:
  index.html: |
    NRF: 5G Network Repository Function - Registry Online
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nrf
  namespace: 5g-core
  labels:
    app: nrf
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nrf
      version: v1
  template:
    metadata:
      labels:
        app: nrf
        version: v1
    spec:
      containers:
      - name: nrf
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "64Mi"
            cpu: "100m"
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: html
        configMap:
          name: nrf-html
---
apiVersion: v1
kind: Service
metadata:
  name: nrf-service
  namespace: 5g-core
  labels:
    app: nrf
spec:
  selector:
    app: nrf
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP